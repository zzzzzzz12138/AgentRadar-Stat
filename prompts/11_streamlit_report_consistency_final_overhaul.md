请继续在当前 AgentRadar-Stat 项目基础上进行“Streamlit 与报告 HTML 同源统一最终大修”。本轮目标是：将此前针对 `AgentRadar-Stat` 报告 HTML 从头到尾形成的全部展示要求、逻辑结构、视觉风格、中文化规范、图表美工、表格排版、Agent 展示方式和文件跳转机制，完整迁移并应用到 Streamlit 中；同时吸收 Streamlit 中较好的可交互功能，反向微调报告 HTML，使两者在逻辑展开、专业水平、美工风格、内容丰富度和交互体验上高度一致。

当前情况：

1. 报告 HTML/PDF 已形成较完整的项目展示结构，包括项目概览、数据采集、指标体系、趋势洞察、评分排行、聚类画像、模型预测、PyTorch 扩展、个性化推荐、DeepSeek Agent、项目亮点、局限展望与附录。
2. Streamlit 已有首页、数据概览、趋势洞察、评分排行、个性化推荐、聚类画像、模型预测、DeepSeek Agent、单项目详情和报告导出页面。
3. 但 Streamlit 当前逻辑目录与报告 HTML 不完全一致，页面内容与美工仍显一般，部分交互未联动，部分图表无标题，部分表格/卡片信息层级不足，DeepSeek Agent 展示内容仍有字段痕迹。
4. 报告 HTML 也应吸收 Streamlit 的部分优点，例如聚类画像页中对 3 类聚类代表项目的展示。

本轮硬约束：

1. 不要重写 GitHub API、评分体系、聚类、模型、PyTorch、个性化推荐和 Agent 主逻辑；
2. 不要破坏 `python main.py`、`pytest`、`streamlit run app.py`；
3. 不得读取、打印、保存或提交 `.env` 中真实 API Key；
4. 不得恢复 `sample_fallback`、`api_live`、`CPU-only`、`available_time`、`api_budget`、`deepseek-v4-flash`、`st.json`；
5. 展示层不得出现原始变量名、调试字段或实现痕迹；
6. 所有面向展示的文字以中文为主，专有名词 GitHub、DeepSeek、AI Agent、PyTorch、PCA、KMeans、TabularWideDeepNet 可保留英文；
7. 所有英文和数字字体为 Times New Roman，中文基准字体为宋体；
8. Streamlit 与报告 HTML 必须使用一致的浅蓝、冰蓝、青蓝科技风；
9. 所有图表、图例、hover、表格、卡片和按钮必须中文化并统一视觉；
10. 最终 PDF 路径必须指向真正的 `outputs/reports/agent_radar_report.pdf`，不能指向错误文件或旧文件。

一、统一 Streamlit 与报告 HTML 的逻辑目录

请将 Streamlit 左侧导航调整为与报告 HTML 目录大体一致，同时保留 Streamlit 的交互优势。

报告 HTML 目录逻辑应为：

1. 项目概览
2. 数据采集
3. 指标体系
4. 趋势洞察
5. 评分排行
6. 聚类画像
7. 模型预测
8. PyTorch 扩展
9. 个性化推荐
10. DeepSeek Agent
11. 单项目详情
12. 项目亮点
13. 局限展望
14. 报告导出 / 附录

Streamlit 导航建议调整为：

1. 首页 / 项目概览
2. 数据采集
3. 指标体系
4. 趋势洞察
5. 评分排行
6. 聚类画像
7. 模型预测
8. PyTorch 扩展
9. 个性化推荐
10. DeepSeek Agent
11. 单项目详情
12. 项目亮点
13. 局限展望
14. 报告导出

要求：

1. Streamlit 目录名称与报告 HTML 基本一致；
2. Streamlit 可以将“模型预测”和“PyTorch 扩展”分开，因为交互页面更适合拆分；
3. Streamlit 的个性化推荐仍作为核心交互功能；
4. 报告 HTML 可吸收 Streamlit 的交互逻辑，在 HTML 中增强可点击跳转、代表项目展示和个性化推荐说明；
5. 所有页面顶部统一使用与报告一致的 Hero 区或紧凑版 Hero 区。

二、统一全局主题与布局

请继续使用或完善：

* `src/streamlit_theme.py`
* `src/streamlit_components.py`
* `src/theme.py`
* `src/ui_components.py`
* `src/agent_display.py`

要求：

1. Streamlit 与报告 HTML 使用同一背景 SVG、卡片圆角、浅蓝边框、浅阴影、玻璃拟态效果；
2. 主色调保持浅蓝/冰蓝/青蓝，不使用红色、绿色、紫色作为主色；
3. 高风险图允许使用橙红色，但其他图表以低饱和蓝色系为主；
4. 英文与数字 Times New Roman，中文宋体；
5. 所有按钮、下载按钮、筛选器、多选标签、radio、slider 使用浅蓝主题；
6. Streamlit 侧栏不再是默认控件风，应与主页面统一；
7. 所有卡片标题有浅蓝短线或渐变下划线；
8. 所有页面说明文字为“内容目的型”，而不是实现说明或修改说明；
9. 不得显示 `api_live`、`sample_fallback`、`Data mode`、`configured`、`value hidden`、`fallback shown`、`Rate limit not collected`、`最近更新`、`生成时间` 等展示层不应出现的词。

三、首页 / 项目概览页大修

当前首页已经有报告风格，但仍需要与报告完全统一，并增强内容层级。

首页结构：

1. 顶部 Hero：

   * `AgentRadar-Stat`
   * `AI Agent 开源生态趋势挖掘、质量评分与智能选题决策平台`
   * `以 GitHub 公开仓库为主数据源，构建从采集、建模到个性化推荐和 Agent 解释的完整闭环。`
2. 功能标签：

   * `500 个 GitHub 公开仓库`
   * `多模型评分`
   * `聚类画像`
   * `PyTorch 表格神经网络`
   * `个性化推荐`
   * `DeepSeek Agent 解释`
   * `HTML/PDF 自动报告`
3. 项目能力流程图：

   * 数据入口 → 特征建模 → 预测扩展 → 智能决策 → 成果交付；
   * 必须是流程图样式，不是单纯卡片堆叠；
   * 与报告首页能力流程图保持一致；
4. 项目概览卡片：

   * GitHub 仓库样本 500；
   * 扩展关键词 34；
   * 去重后仓库；
   * 入模项目 500；
   * A 级推荐项目；
   * 高风险项目；
   * 聚类画像；
   * 监督任务；
   * 机器学习模型；
   * 表格宽深神经网络；
   * 个性化推荐；
   * DeepSeek Agent；
5. 注意 `TabularWideDeepNet` 不得截断，可显示为 `表格宽深神经网络`，副标题写 `TabularWideDeepNet`。

首页精选卡片墙：

1. 保留精选项目卡片墙；
2. 在卡片墙最开头加入 `查看单项目评分构成` 功能；
3. 用户可选择一个项目后，直接跳转或展开该项目的七维评分构成；
4. 卡片墙按综合潜力分排序；
5. 卡片中仓库名可点击 GitHub；
6. 卡片展示：名次、仓库名、项目主题、语言、Stars、Forks、综合潜力分、风险分、项目类型。

四、数据采集页

将当前“数据概览”改为与报告 HTML 中“数据采集”一致。

页面标题：`数据采集`

页面说明：
`本页展示 GitHub 公开仓库生态样本从关键词搜索、种子仓库补充、去重过滤到特征入模的形成过程，体现数据来源、筛选逻辑与样本构建的可复核性。`

要求：

1. 保留样本构建 KPI；
2. 数据采集漏斗图使用四步：

   * 关键词搜索候选；
   * 去重后仓库；
   * 相关性过滤后；
   * 最终入模项目；
3. 漏斗图 hover 中文化；
4. 不显示 `api_live`、`sample_fallback`、API token、rate limit、fallback reason；
5. `高级诊断` 如保留，必须默认折叠；
6. 增加“样本构建路径说明”卡片：

   * 关键词覆盖 Agent、RAG、MCP、Coding、Workflow 等方向；
   * 多排序策略覆盖 stars、updated、forks、best match；
   * seed 仓库补充高相关项目；
   * full_name 去重；
   * 相关性过滤后入模。

五、指标体系页

新增或完善单独的 `指标体系` 页面，使 Streamlit 与报告目录一致。

页面说明：
`指标体系把开源项目的热度、维护活跃、社区参与、文档质量、技术创新、复现可行性与风险惩罚整合为统一评价框架，避免仅按 Stars 排序。`

内容：

1. 七维评分体系卡片：

   * 趋势热度；
   * 活跃维护；
   * 社区参与；
   * 文档质量；
   * 技术创新；
   * 复现可行；
   * 风险惩罚；
2. 个性化匹配机制卡片：

   * 用户画像；
   * 语言偏好；
   * 主题偏好；
   * 风险偏好；
   * 输出偏好；
3. 可加入评分公式的可视化表达；
4. 不直接显示原始变量名。

六、趋势洞察页修复交互失效

当前趋势洞察页中 `展示数量` 滑块变化后，主要编程语言分布图和 GitHub Stars 分布特征图没有联动。请修复：

1. `展示数量` 应控制当前页面所有相关图表和卡片墙的数据范围；
2. 主要编程语言分布应基于 Top N 或筛选后的数据重新计算；
3. GitHub Stars 分布特征应基于 Top N 或筛选后的数据重新计算；
4. Stars-Forks 气泡图、技术主题分布和精选卡片墙也应使用同一筛选后的数据；
5. 若需要保留“全量视角”，请增加切换：

   * `全量样本`
   * `当前展示数量`
6. 默认以当前展示数量联动。

趋势页卡片墙：

1. 在精选项目卡片墙最开头加入 `查看单项目评分构成` 功能；
2. 用户选择项目后展示七维评分进度条或跳转至单项目详情；
3. 卡片墙按综合潜力分排序；
4. 卡片布局与首页保持一致。

趋势页图表：

1. 每张图必须有清晰标题；
2. 图名后说明为内容目的型：

   * 主要编程语言分布：观察项目复现门槛和生态主语言；
   * GitHub Stars 分布特征：观察头部项目与长尾项目差异；
   * Stars-Forks 气泡关系：观察关注度与派生开发活跃度；
   * 技术主题分布：观察样本中不同 AI Agent 技术方向结构；
3. 不得显示原始变量名；
4. hover 美化并中文化。

七、评分排行页优化

当前评分排行页的风险-潜力四象限做得较好，但缺标题，且 `查看单项目评分构成` 应前移。

要求：

1. 风险-潜力四象限图上方必须有图表卡片标题：

   * `风险—潜力四象限`
   * 说明：`将项目放在风险分与综合潜力分构成的坐标系中，帮助识别优先推荐、谨慎探索和暂不推荐的项目。`
2. 在精选卡片墙最开头放置 `查看单项目评分构成`；
3. 不再把该功能放在页面末尾；
4. 筛选器保持可交互：

   * 推荐等级；
   * 主要语言；
   * 项目类型；
   * 风险分范围；
   * 潜力分范围；
   * 项目数量；
5. 筛选器应是两行或卡片式布局，不要拥挤；
6. 卡片墙按当前筛选结果动态更新；
7. 单项目评分构成展示：

   * 趋势热度；
   * 活跃维护；
   * 社区参与；
   * 文档质量；
   * 技术创新；
   * 复现可行；
   * 风险惩罚；
8. 所有图例与 hover 中文化。

八、个性化推荐页优化

当前个性化推荐页的卡片墙是优点，但推荐解释入口应前移。

要求：

1. 在个性化推荐卡片墙最开头放置 `查看推荐解释`；
2. 用户选择某个项目后展示：

   * 为什么适合你；
   * 推荐理由；
   * 风险提示；
   * 三天复现路线；
   * Codex Prompt；
3. 该解释卡必须全宽展示，不得出现竖排；
4. 推荐理由必须自然成句，不使用引号列表、机械片段或 `CPU-only`；
5. 个性化推荐 Top 15 图保留；
6. 个性化分 vs 通用潜力分图保留；
7. 图表 hover 中文化；
8. 卡片墙按个性化匹配分排序；
9. 卡片中显示个性化匹配分、通用潜力分、风险分、项目主题、语言、项目类型、GitHub 链接；
10. 页面顶部用户画像摘要与侧栏画像保持同步。

九、聚类画像页优化，并反向优化报告 HTML

Streamlit 聚类画像页中“按聚类类型罗列代表项目”是优点。请将这一点保留并进一步优化，同时把该展示方式同步到报告 HTML 的聚类画像章节。

Streamlit 聚类画像页：

1. 第一张图必须标明标题：

   * `PCA / 聚类项目地图`
   * 说明：`将多维项目特征压缩到二维空间，观察不同项目画像之间的距离和聚集关系。`
2. 不显示原始字段名；
3. hover 中文化；
4. 点击点打开 GitHub；
5. 聚类指标卡中文化；
6. 聚类类型数量图中文化；
7. 聚类方法比较表中文化；
8. 每类代表项目展示为卡片墙：

   * 高 star 低维护风险型；
   * 文档完善可复现型；
   * 小众垂直工具型；
     或当前实际 cluster_name；
9. 每类代表项目卡片显示：仓库名、主题、语言、潜力分、风险分、GitHub 链接；
10. 不再使用大表格导致竖排。

报告 HTML 同步优化：

1. 在报告 HTML 的聚类画像章节中，增加“各聚类类型代表项目”模块；
2. 每类展示 Top 3—5 代表项目；
3. 使用与 Streamlit 相同的卡片风格；
4. 保持报告 HTML 与 Streamlit 内容一致。

十、模型预测页优化，并反向优化报告 HTML

当前模型预测页存在任务 A/B 区分不明显、特征重要性图任务差异不清、PyTorch 训练曲线无标题且颜色过花的问题。请修复。

模型预测页结构：

1. 任务定义卡：

   * 任务 A：评分体系代理模型；
   * 任务 B：项目潜力代理预测；
   * 防泄漏说明；
2. 分别展示任务 A 与任务 B：

   * 建议使用两个 tab：

     * `任务 A：评分体系代理模型`
     * `任务 B：项目潜力代理预测`
   * 或上下两个全宽分区；
   * 不要混在一起让用户第一眼难以区分。
3. 每个任务分区包括：

   * 最佳模型卡；
   * 指标卡；
   * 模型对比图；
   * 模型结果表；
   * 特征重要性图。
4. 特征重要性图必须明确区分任务 A/B：

   * 如果放在同一图，必须有图例；
   * 更推荐分成两个 tab 或两个图；
   * 颜色使用低饱和蓝色系；
   * 不使用紫色或高饱和杂色。
5. PyTorch 训练曲线：

   * 必须有标题：`PyTorch 表格宽深神经网络训练曲线`;
   * 线条颜色使用低饱和蓝色系；
   * 不使用红绿紫杂色；
   * 图例中文化；
   * 说明：`训练曲线用于观察模型收敛过程和验证集表现。`
6. 所有模型名中文化：

   * 逻辑回归；
   * 随机森林；
   * 极端随机树；
   * 梯度提升树；
   * 校准随机森林；
   * 随机基线；
7. 特征名中文化；
8. 不显示 `calibrated_random_forest`、`agent_relevance_score` 等原始字段。

报告 HTML 同步优化：

1. 报告 HTML 的模型预测章节学习 Streamlit 的任务 A/B 分区展示；
2. 特征重要性图明确区分任务 A/B；
3. PyTorch 训练曲线标题与低饱和蓝色系同步。

十一、DeepSeek Agent 页重点重做

当前 DeepSeek Agent 页面问题最大，核心判断、建议动作、相关依据、风险审查中显示 `source_type user_profile`，完全不适合展示。请重点修复。

要求：

1. 不显示 `source_type`、`user_profile`、`api_status`、`readme_fetch_status` 等字段名；
2. 不显示 fallback、generated explanation、未显式给出、字段依据等模板痕迹；
3. 每个智能体的内容必须与单项目详情中的智能体展示一致，即自然中文、解释清楚、无调试痕迹；
4. 每个 Agent 卡片全宽展示；
5. 每个 Agent 卡片结构：

   * 智能体职责；
   * 核心判断；
   * 相关依据；
   * 建议动作；
   * 风险提醒。
6. 若 DeepSeek 输出缺失，必须使用规则生成正式中文解释，而不是把字段名塞进去。
7. 七个 Agent 中文名称：

   * 采集策略智能体；
   * 数据质量诊断智能体；
   * 主题识别智能体；
   * 评分解释智能体；
   * 个性化项目顾问智能体；
   * 报告生成智能体；
   * 事实审查智能体。
8. ProjectAdvisorAgent 需要展示：

   * 当前用户画像摘要；
   * Top 5 推荐项目；
   * 推荐理由；
   * 三天复现路线；
   * Codex Prompt。
9. CriticAgent 需要展示：

   * 审查结论；
   * 已绑定的数据依据；
   * 需要谨慎表达的地方；
   * 如何避免夸大。
10. 页面不得显示 JSON；
11. 页面不得显示字段名残留；
12. Agent 卡片风格与报告 HTML 一致。

同时同步报告 HTML：

1. 如果报告 HTML 的 DeepSeek Agent 章节仍存在类似字段痕迹，也一并修正；
2. 报告 HTML 与 Streamlit 的 Agent 展示内容逻辑保持一致。

十二、单项目详情页优化

当前单项目详情页整体不错，但 README 信号卡出现文字过大、断行不自然，例如 `README 提到 GPU 或密钥要求` 被拆得很难读。

要求：

1. README 信号卡文字适当缩小；
2. 卡片宽度更合理；
3. 不要让 `README`、`GPU` 等词逐字换行；
4. 信号卡可改为两行三列或图标标签式展示；
5. 七维评分条保留；
6. Agent 解释按钮保留；
7. 解释内容与 DeepSeek Agent 页一致；
8. 不显示原始字段名；
9. GitHub 链接明显可点击。

十三、报告导出页与报告 HTML 附录修复 PDF 跳转

当前 Streamlit 报告导出页与报告 HTML 附录中有关 PDF 项目报告的跳转文件不是真正的 PDF 项目报告。真正 PDF 路径应为：

`C:\Users\hp\OneDrive\Desktop\大学\课程期末\python程序设计\AgentRadar-Stat\outputs\reports\agent_radar_report.pdf`

项目内相对路径应为：

`outputs/reports/agent_radar_report.pdf`

请修复：

Streamlit 报告导出页：

1. PDF 下载按钮必须读取 `outputs/reports/agent_radar_report.pdf`;
2. HTML 报告按钮读取 `outputs/reports/agent_radar_report.html`;
3. 不再链接错误文件；
4. 不显示本地绝对路径；
5. 如 PDF 不存在，提示：
   `请先运行 python main.py 生成项目报告。`
6. 页面按钮文案：

   * `打开 / 下载项目报告 PDF`
   * `打开交互式 HTML 项目报告`
7. 不突出 HTML 大屏。

报告 HTML 附录：

1. PDF 报告链接必须指向 `outputs/reports/agent_radar_report.pdf`;
2. HTML 报告链接指向 `outputs/reports/agent_radar_report.html`;
3. 删除错误链接；
4. 删除 HTML 大屏行；
5. 路径使用相对路径；
6. 链接可点击。

十四、交互与报告之间的双向融合

请将 Streamlit 的长处反向应用到报告 HTML：

1. 聚类画像中增加各聚类代表项目；
2. 评分排行可增加“单项目评分构成说明”模块；
3. 个性化推荐可增加“用户画像摘要 + 推荐解释”模块；
4. 模型预测可学习 Streamlit 的任务 A/B 交互结构，在报告中更清晰区分任务；
5. DeepSeek Agent 卡片内容与 Streamlit 保持一致；
6. 报告 HTML 中所有仓库名链接继续可点击 GitHub；
7. 报告 HTML 不需要增加复杂筛选器，但可以增强点击、hover 和内容结构。

十五、全局残留扫描

最终 Streamlit 页面和报告 HTML 可见文本中不得出现：

1. `sample_fallback`
2. `sample fallback`
3. `api_live`
4. `CPU-only`
5. `CPU only`
6. `recommendation_level`
7. `risk_level_cn`
8. `cluster_name`
9. `pca_x`
10. `pca_y`
11. `stars_total`
12. `forks_total`
13. `risk_score`
14. `final_potential_score`
15. `personalized_score`
16. `calibrated_random_forest`
17. `agent_relevance_score`
18. `has_demo`
19. `has_requirements`
20. `source_type`
21. `user_profile`
22. `fallback shown`
23. `generated explanation`
24. `value hidden`
25. `configured`
26. `Data mode`
27. `Rate limit not collected`
28. `最近更新`
29. `生成时间`

注意：这些词可以作为源码变量名存在，但不得作为最终页面可见文本出现。

十六、运行验证

完成后请运行：

1. `python main.py`
2. `pytest`
3. `streamlit run app.py`

检查：

1. Streamlit 导航是否与报告目录逻辑一致；
2. 首页是否不再像调试面板；
3. 数据采集页是否替代 API 状态页；
4. 指标体系页是否新增；
5. 趋势洞察的展示数量是否联动所有图表和卡片墙；
6. 评分排行页是否把单项目评分构成移到卡片墙开头；
7. 个性化推荐页是否把推荐解释移到卡片墙开头；
8. 聚类画像页是否保留并优化代表项目展示；
9. 报告 HTML 是否同步增加聚类代表项目；
10. 模型预测页是否清晰区分任务 A/B；
11. 特征重要性图是否清晰区分任务 A/B；
12. PyTorch 训练曲线是否有标题且为蓝色系；
13. DeepSeek Agent 是否彻底去除 `source_type user_profile` 等字段痕迹；
14. 单项目详情页 README 信号是否不再竖排；
15. 报告导出页和报告 HTML 附录 PDF 链接是否指向真正的 `outputs/reports/agent_radar_report.pdf`;
16. 是否无 API Key 泄露风险。

十七、完成后汇报

请按以下格式汇报：

1. 本轮修改了哪些文件；
2. Streamlit 导航如何调整以匹配报告目录；
3. 首页如何优化；
4. 数据概览如何改为数据采集；
5. 是否新增指标体系页；
6. 趋势洞察页展示数量联动是否修复；
7. 评分排行页单项目评分构成是否前移；
8. 个性化推荐页推荐解释是否前移；
9. 聚类画像代表项目如何优化，并是否同步到报告 HTML；
10. 模型预测任务 A/B 是否区分清晰；
11. 特征重要性图是否区分任务 A/B；
12. PyTorch 训练曲线是否有标题且颜色统一；
13. DeepSeek Agent 页面是否修复 `source_type user_profile` 等内容问题；
14. 单项目详情页 README 信号卡是否优化；
15. PDF 跳转路径是否修复为 `outputs/reports/agent_radar_report.pdf`;
16. 报告 HTML 附录是否同步修复；
17. 全局残留词扫描是否通过；
18. `python main.py` 是否通过；
19. `pytest` 是否通过；
20. `streamlit run app.py` 是否可运行；
21. 是否存在 API Key 泄露风险；
22. 仍需人工检查的页面。

请继续在当前 AgentRadar-Stat 项目基础上进行“最终报告封版微调”。本轮只聚焦 `outputs/reports/agent_radar_report.html` 与自动导出的 `outputs/reports/agent_radar_report.pdf`，不再改数据采集、评分、聚类、模型训练、PyTorch、个性化推荐和 DeepSeek Agent 主逻辑。

当前报告整体已经较成熟，但仍有少量展示层问题需要最终修正。本轮目标是消除所有“内部修改要求痕迹”“变量名痕迹”“卡片单调痕迹”和“页脚冗余痕迹”，让报告成为可直接展示的最终版项目成果页。

硬约束：

1. 不要重写 GitHub API、评分体系、聚类、模型、PyTorch、个性化推荐、Agent 主逻辑；
2. 不要破坏 `python main.py`、`pytest`、`streamlit run app.py`；
3. 不得读取、打印、保存或提交 `.env` 中真实 API Key；
4. 不得恢复 `sample_fallback`、`api_live`、`CPU-only`、`available_time`、`api_budget`、`deepseek-v4-flash`、`st.json`；
5. 展示层不得出现“答辩讲法”“生成时间”“最近更新”“数据、模型与可视化由项目流水线自动生成”等不适合展示的文字；
6. 所有面向报告读者的说明文字必须是“内容目的型”，不能像是在解释修改要求；
7. 所有英文和数字字体保持 Times New Roman，中文基准字体保持宋体；
8. 修改后重新生成 HTML 和 PDF。

一、首页 Hero 与项目能力雷达最终重排

当前首页中，项目能力雷达卡仍在右侧分栏展示。请改为：

1. 顶部大标题 `AgentRadar-Stat` 与副标题继续占满水平；
2. 其下的项目定位说明也占满水平：
   `以真实 GitHub 公开仓库为主数据源，构建从采集、建模到个性化推荐和 Agent 解释的完整闭环。`
3. 项目关键词标签紧跟其后，占满水平展示；
4. `项目能力雷达卡` 不再右侧分栏，而是作为一个全宽流程化模块展示；
5. 雷达卡改成“能力流程图”样式，而不是普通小卡片列表。

能力流程图建议结构：

第一层：数据入口

* GitHub 公开仓库
* 关键词搜索
* seed 仓库补充

第二层：统计建模

* 特征工程
* 七维评分体系
* 聚类画像
* Task A / Task B 预测

第三层：智能决策

* PyTorch 表格神经网络
* 用户画像匹配
* DeepSeek Agent 解释

第四层：成果交付

* HTML 报告
* PDF 报告
* Streamlit 交互入口

视觉要求：

1. 使用横向或阶梯式流程图；
2. 节点之间有箭头或数据流线；
3. 每层使用浅蓝、冰蓝、青蓝的渐变；
4. 不要再使用 01/02/03 纵向状态列表；
5. 节点内文字简洁；
6. PDF 中不要被分页截断；
7. 整体宽度与正文卡片宽度一致。

二、项目概览卡片最终修正

当前项目概览中的 `TabularWideDeepNet` 仍显示不完整。请修正：

1. `TabularWideDeepNet` 可以换行显示；
2. 或主标题显示为 `表格宽深神经网络`，副标题显示 `TabularWideDeepNet`；
3. 不得被卡片边界裁切；
4. 如果卡片中仍有单字母图标或不明所以的小图标，请全部删除；
5. 项目概览卡片应保持整齐网格，不要出现单行孤立或卡片高度不一致；
6. 所有卡片说明文字颜色使用深蓝灰，字号比当前略大，增强可读性。

三、执行摘要卡片图标处理

当前执行摘要中部分小图标含义不明确。请二选一处理：

方案 A：替换为明确语义图标

* 真实采集规模：数据库 / 云端节点；
* 评分体系：仪表盘 / 评分尺；
* 聚类画像：节点网络；
* 预测建模：模型曲线 / 芯片；
* PyTorch 扩展：神经网络；
* 个性化推荐：用户画像 / 指南针。

方案 B：直接删除所有摘要小图标
如无法保证图标语义匹配，则直接删除图标，改用编号或左侧蓝色短线增强层级。

要求：

1. 不保留不明所以的符号；
2. 每张摘要卡标题、发现、数字证据排版更清晰；
3. 卡片之间留白一致；
4. 不要影响 PDF 分页。

四、全报告章节说明与图表说明改为“内容目的型”

请逐章扫描 `agent_radar_report.html` 生成逻辑，删除所有像修改要求、实现说明、内部提示的句子。所有章节标题下的说明、图名前后的说明，都必须改为面向读者的“本章看什么、为什么重要、图表说明什么”。

请重点替换以下类型文本：

1. 删除或改写类似：

   * “只保留搜索候选、去重、相关性过滤和最终入模四个核心步骤”
   * “仓库级图表在 HTML 中支持点击跳转 GitHub”
   * “Top 榜单只显示仓库名，完整 owner/name 和链接放在 hover 中”
   * “对数坐标避免头部项目压缩长尾项目，虚线为中位数参考线”
   * “将 Agent、MCP、RAG、编程智能体、工作流等主题统一中文化”
   * “风险-潜力四象限和 PCA 地图均为全宽展示”
   * “横向中文标签避免 PDF 中竖排断裂”
   * “模型名全部中文化”
   * “将原始特征名映射为中文解释名”

2. 替换为自然报告表述，例如：

项目概览：
`AgentRadar-Stat 将 GitHub 公开仓库采集、特征工程、评分体系、聚类画像、监督模型、PyTorch 表格神经网络、个性化推荐与 DeepSeek Agent 解释整合为一个可复现的 Python 数据科学项目。`

数据采集：
`本章展示 GitHub 公开仓库生态样本从关键词搜索、种子仓库补充、去重过滤到特征入模的形成过程，体现数据来源、筛选逻辑与样本构建的可复核性。`

数据采集漏斗：
`数据采集漏斗展示候选仓库到最终入模样本的筛选路径，用于说明样本规模、去重处理和相关性过滤如何共同形成建模数据集。`

指标体系：
`指标体系把开源项目的热度、维护活跃、社区参与、文档质量、技术创新、复现可行性与风险惩罚整合为统一评价框架，避免仅按 Stars 排序。`

趋势洞察：
`趋势洞察用于观察 AI Agent 相关项目的语言生态、关注度长尾分布、派生开发活跃关系和技术主题结构。`

主要编程语言分布：
`主要编程语言分布用于观察项目复现门槛和生态主语言。`

GitHub Stars 分布特征：
`GitHub Stars 分布特征使用对数分箱展示头部项目与长尾项目的差异。`

Stars-Forks 气泡散点图：
`Stars-Forks 气泡散点图展示项目关注度与派生开发活跃度的关系，帮助区分高关注项目和高复用项目。`

技术主题分布：
`技术主题分布展示样本中 AI Agent 框架、MCP 工具生态、检索增强生成、编程智能体和工作流自动化等方向的结构。`

评分排行：
`本章从综合潜力与风险暴露两个角度识别开源项目：前者回答哪些项目值得优先关注，后者回答哪些项目需要谨慎评估。`

综合潜力 Top 20：
`综合潜力 Top 20 展示评分体系下最值得优先关注的仓库，便于快速识别高热度、高活跃度且具备复现价值的项目。`

高风险项目 Top 20：
`高风险项目 Top 20 展示在维护压力、复现复杂度或项目稳定性方面需要谨慎评估的仓库。`

聚类画像：
`聚类画像用于把仓库划分为不同项目类型，帮助理解项目在潜力、风险和工程成熟度上的差异。聚类结果用于解释项目结构，不作为监督学习标签。`

风险-潜力四象限：
`风险-潜力四象限把项目放在同一张坐标图中，帮助判断哪些项目值得优先复现，哪些项目虽然相关但风险较高。`

PCA / 聚类项目地图：
`PCA 项目地图将多维特征压缩到二维空间，用于观察不同项目画像之间的距离和聚集关系。`

聚类类型数量图：
`聚类类型数量图展示不同项目画像在样本中的分布情况。`

模型预测：
`模型预测章节用于检验评分体系的可解释性，并通过代理标签任务观察哪些原始特征能够解释项目潜力。`

任务 A / 任务 B 模型对比：
`任务 A 检验评分体系边界，任务 B 面向项目潜力代理预测；二者共同用于说明模型结果不是简单复刻最终评分。`

特征重要性：
`特征重要性展示不同模型在判断项目潜力时主要依赖哪些仓库特征。`

PyTorch 扩展：
`本章展示表格宽深神经网络如何把数值特征、类别特征和线性记忆分支组合起来，作为传统机器学习模型之外的深度学习扩展实验。`

个性化推荐：
`个性化推荐把通用潜力分转化为面向用户画像的匹配分，使系统能够根据目标、语言偏好、主题偏好和风险偏好给出差异化项目建议。`

DeepSeek Agent：
`DeepSeek Agent 负责把字段、图表和模型结果转化为可读解释，帮助用户理解推荐理由、风险来源和下一步操作。`

项目亮点：
`本章总结项目在真实数据、统计建模、深度学习扩展、个性化推荐、多智能体解释和多形态交付方面的综合能力。`

局限与展望：
`本章说明项目边界和后续扩展方向，避免把代理标签和解释层输出误读为绝对结论。`

五、局限与展望图标处理

当前“研究边界”和“后续扩展”中的 `!` 与 `+` 图标不够美观。请二选一处理：

方案 A：替换为明确语义图标：

* 研究边界使用低饱和蓝灰色“边界 / 提醒 / 盾牌 / 信息”图标；
* 后续扩展使用“上升箭头 / 路线 / 扩展节点 / 加号圆标”图标；
* 图标必须统一样式，不能像普通字符。

方案 B：直接删除 `!` 与 `+`，用浅蓝色左边框、编号或小圆点替代。

要求：

1. 不使用孤立感叹号和加号；
2. 列表行距更舒展；
3. 研究边界与后续扩展的视觉层级保持一致；
4. PDF 中不显得像临时符号。

六、页脚最终调整

删除 HTML 和 PDF 最末尾的：

`· 数据、模型与可视化由项目流水线自动生成`

页脚最终只保留简洁表达，例如：

`AgentRadar-Stat · GitHub 开源生态智能决策平台`

或：

`AgentRadar-Stat · AI Agent 开源生态趋势挖掘与智能选题决策平台`

不要出现：

1. 生成时间；
2. 最近更新；
3. 数据模式；
4. API Key 说明；
5. 数据、模型与可视化由项目流水线自动生成。

七、全局小卡片美工最后优化

报告中仍有一些普通小卡片比较单调，请统一做轻量提升：

1. 卡片增加更细腻的浅蓝边框；
2. 卡片 hover 在 HTML 中轻微上浮；
3. 卡片标题加浅蓝短线或渐变下划线；
4. 卡片内容行距更舒展；
5. 关键数字使用 Times New Roman，并略微加粗；
6. 图表卡片与文字卡片风格统一；
7. 不要新增过度花哨动画，保持正式项目风格。

八、最终残留扫描

完成后请扫描最终 HTML 可见文本，确保不再出现以下不适合最终展示的内容：

1. `sample_fallback`
2. `sample fallback`
3. `api_live`
4. `CPU-only`
5. `CPU only`
6. `答辩讲法`
7. `生成时间`
8. `最近更新`
9. `数据、模型与可视化由项目流水线自动生成`
10. `recommendation_level`
11. `risk_level_cn`
12. `cluster_name`
13. `task_b_proxy_good_project`
14. `calibrated_random_forest`
15. `agent_relevance_score`
16. `has_demo`
17. `has_requirements`

注意：这些词可作为源码变量名存在，但不得出现在最终 `agent_radar_report.html` 和 PDF 可见文本中。

九、运行与验证

完成后运行：

1. `python main.py`
2. `pytest`
3. 重新生成 `outputs/reports/agent_radar_report.pdf`

检查：

1. 首页项目能力雷达是否已全宽流程化；
2. 项目概览中 `TabularWideDeepNet` 是否完整显示；
3. 执行摘要图标是否已语义化或删除；
4. 章节说明和图表说明是否全部改为内容目的型；
5. 局限与展望中是否删除或替换 `!` 与 `+`；
6. 页脚是否删去“数据、模型与可视化由项目流水线自动生成”；
7. 最终 HTML/PDF 是否无残留词；
8. 是否无 API Key 泄露风险。

十、完成后汇报

请按以下格式汇报：

1. 本轮修改了哪些文件；
2. Hero 与项目能力雷达如何调整；
3. 项目概览中 `TabularWideDeepNet` 是否完整显示；
4. 执行摘要图标如何处理；
5. 章节说明和图表说明是否已全部改为内容目的型；
6. 局限与展望图标如何处理；
7. 页脚是否已删除冗余文字；
8. 小卡片美工做了哪些统一优化；
9. 最终 HTML 是否重新生成；
10. 最终 PDF 是否重新生成；
11. `python main.py` 是否通过；
12. `pytest` 是否通过；
13. 残留扫描是否通过；
14. 是否存在 API Key 泄露风险；
15. 是否还有必须人工检查的细节。
